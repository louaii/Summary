API(Application Programming Interface) allows communication between the client-side and the server-side which the database is stored into
Through GET/POST/PUT/DELETE http requests
GET: to retrieve data(shown in http request)
POST: to create - retrieve - update - delete (hidden and used for security when validating password)
PUT: update data
DELETE: delete data

restAPI uses the CRUD requests:
CREATE - READ - UPDATE - DELETE

in Laravel:
php artisan install:api
go to routes directory
api.php

syntax
//class.http_request_api_method_function_inclass('web_page/', function(){return;});

Route::https_request_api_method('web_page/', function(){
	return 'string to print';
});

//example
Route::get('welcome', function (){
	return 'Welcome to API';
});

GET http://127.0.0.1:8000/api/page_in_route

imports:
use App\Http\Controllers\nameController;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;

each time controller added make sure to import it's class

to make api count the rows used
Route::http_request('page/{counter}', [Controller_name::class, 'function_in_Conrtoller']);

Route::get('user/{id}', [UserController::class,'CheckUser']);